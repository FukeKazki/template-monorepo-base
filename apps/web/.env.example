# このファイルを .env.local にコピーして使う（.env.local は *.local として gitignore 対象）。
# USE_MOCK=false にすると MSW を止め、apps/api のローカルWorkerへ実際に接続する。
# 未設定またはfalse以外なら常にMSWモックを使う。
# USE_MOCK=false

# USE_MOCK=false 時の /api プロキシ転送先（省略時 http://localhost:8787）
# VITE_API_PROXY_TARGET=http://localhost:8787
